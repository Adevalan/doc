jre所带的lib，和C/C++编译器所附带的基础类库是同样的一个概念。
除语言支持以外(JVM hotspot vm)，还附带给予其语言支持所实现的一系列基础类库。(通用的)
这些类库不需要在用户包内部署，在系统(OS)运行时里自制。
用户的程序包不需要附带这些基础程序包，只需要关注于用户自己编写的libs来部署项目。
生成的项目中也不需要依赖于特定版本的程序语言包。更不需要带上特定版本的程序语言包。
因为默认假设目标机器上已经存在有该包，因为该机器需要安装jre才能运行该程序式，因此总会带有该系列程序jar包的。