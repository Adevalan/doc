一些修饰词并不包含在C++的语法里，
而是实现编译器特定的功能。
这一块的确是编译器的功能。
指定编译器如何编译代码，编译出什么代码。
比如__asm{
 mov eax,00800000H
 iret
}
这些语句是指定编译器编译出代码，而不包含在C++的部分里。
也就是说，编译器就是编译器，没有纯C++的编译器。

C++包含语法、头文件引用、装配器（编译指明）部分。
但有一些关键字可能不属于微软的拓展，而别的编译器也有的。
而有些修饰词是Microsoft专用且特有的。也有专利的修饰词。