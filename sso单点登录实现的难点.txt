SSO单点登录难点在于：
如何解决ticket在多个应用中的传递，
可使用如下方式...
地址栏传递
post传递
jsonp跨域设置cookies
同父域单点
即，该pkey信息一定是保留在客户端浏览器，并被多个站点访问到。
多个站点每个站点都会读取浏览器的pkey信息，因为它必须具有映射才能够维持。
而设置cookies的方式，可以是客户端浏览器来设置，也可以是跨域了来设置，
可以经过浏览器后设置，也可以不经过浏览器再设置，
但不通过再请求的方式进行设置，服务端发起的setCookies不能被浏览器响应。