总结几种方式：
①规划一个空间用来存储某表的所有数据。
②block数据产生自描述，自己索引到表的描述上。也就不需要“用户实际表空间”的概念了。
数据库的运作方式又根据一些system数据库来运作。因此有一些表的DDL的关系是存储在System表空间下
的，而数据本身又描述了自己是属于哪个表空间下的。或者也可以没有这些信息，当一个物理范围包含了
一个表的所有数据时。
extent真的就是数据区块。如果当前数据区块当中存放了多个表的数据，那么这些表项的entity数据将会
描述自己是属于哪个表的。