按照本文的意思，前台进程组直接与终端对接。
当终端有输入输出时，都传递给该会话的前台进程组。

因此可以说是会话对接终端，但终端由可以在这个会话内自由选择对接的进程。


如果A程序作为了前台进程，但是它屏蔽了Ctrl+Z，那么这个进程作为前台进程组被对接到终端，
那么终端怎么关闭这个程序？？？？


另外按照我以前的理解是，控制信息传递到bash，再由bash发给前台的进程组上。

在这个session内打开的所有进程，都叫做这个session的进程。
而这个session。这个session内，有分了前台进程组和后台进程组。

