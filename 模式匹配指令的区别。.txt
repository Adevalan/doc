模式匹配指令，匹配到指令。

非模式匹配指令，不在输入字符时匹配。
而是回车匹配。

dd就是模式匹配指令。
输入一个d不代表任何作用。
d为组合指令，因此不算一个单独指令。

d匹配任意多个？？？？？？？？？？
dxd
d 后面跟着字符。


输入字符时，长匹配符合时，自动忽略掉短匹配。
d没有短匹配。
dw是长匹配，但是w

也就是说，其实是把整个输入缓冲区的所有字符，和^{w}$进行匹配。
符合哪个是哪个。
