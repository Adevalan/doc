安装程序都是差不多的，
都是安装到特定的目录下。


而执行程序上有些差别。NSIS(msi)可能和linux的deb是一样的。msi不装NSIS时，
就是一段脚本文件，由Nsis安装程序来执行。因此会提示不能同时启动两个msi安装实例。


Linux是用通用的安装程序安装，deb或者rpm。
不是两种包的，有自己的安装程序，或者没有自己的安装程序。
有的话，一般是make install，自己写的安装程序，不想打包到deb。
当然也可以像eclipse那样的elf安装程序。这一切都是高度自由式的。

没有的话，一般就是直接编译出来的。