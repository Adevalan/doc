ifdef进行预编译。

设置工程选项将会对C源文件产生一定影响。
工程选项不但会影响链接期间，同时会影响c源文件。

工程选项在全生命周期可用。

其实也就是命令行选项。只不过以可视化的方式设置出来了。


Cl.exe执行的参数~

命令行会定义一些特殊的预编译宏。

but，它将在IDE编译之前被解析。
即，编译时产生的条件。
预编译就是操作一些源文件。

设计编译器需要制定一系列规则来明确用户的指令。
只要用户符合这种规则，编译器就明确了用户需要做什么。