外键只是说，我所填写的东西，必须在别的表里头找的到内容。
（以本表某个字段作外键约束，约束到目标表的主键去）
（为什么不可以是其它键，比如目标表的非主键。这个非主键不能作为该条记录的
标识，标识该记录就必须有该记录的唯一标识主键。没有标记该字段为唯一标识时，该记录就不存在唯一标识。）

主键约束只是说，我所填写的东西，可以被其它的表搜寻的到，并以主键为唯一序列。
主键约束与外键约束另有用途。
外键必须与主键配合使用。