控制字符是一个标准，不同的解析方实现它的方法不一样。
比如\ 0x27这种字符就是交给编译器来解析的，编译出最终的结果。
而其它的设备，也可以用0x27来达到一些控制目的。这种一般会出现在字符设备上。
块状设备分块解析却没有处理机会，除非特定的去处理。
而字符设备可以针对每一个输入的字符采用一个字符循环处理。

键盘上一般存在着很多控制字符。
键盘就是一个字符输入设备，系统就是这个字符输入设备控制字符的处理器。