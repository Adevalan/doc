用户登录之前可否访问终端？
Putty仅仅是一个终端的对接程序，负责输入输出终端所要的内容。
终端负责输出输出系统的信息，
该终端就是bash吗？

bash在登录之前就启动了么。。。。。。。
登录之前启动的进程肯定有，就像windows一样，在登录之前已经有很多的服务在运行了。

but,但是bash是属于什么用户的呢？

为了兼容那个概念一个进程属于一个用户，因此需要将那些未归类的用户独立到系统用户中。
