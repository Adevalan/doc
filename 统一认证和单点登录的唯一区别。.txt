统一认证就是，你和我都使用同一个接口登录，
不在本地进行认证。

单点登录就是我的登录是我的接口，你的登录是你的接口，但我们可以互相进行认证验证。

单点登录，A用户在A地点登录，使用A网站的唯一用户信息，登录成功后，即可代表另一个应用所绑定的账号了。即，不共享数据库，但可以映射数据库，
同一个ticket在不同的应用代表不同的用户。

统一认证是，A用户在A应用登录，也可以在B应用登录，几个应用共同使用同一个账号数据库。