如果它是个数组，那么必要有元素声明，那么其它部分将会被当做元素声明。
即：一个标识符只能有一种主类型，或说只能有一种类型。不能填写多种，
虽然你填写了多种，但只能有一种主类型，其它只是这些主类型必须要的附带品。

甚至可能是嵌套的：
例如声明了一种类型，需要另一种类型，又需要另一种类型，
但只有一种作为主类型，每一种父类型都将做为下一个类型的主类型。
因此以这个类型树出发点，永远只有一个主类型，其它都是树状的附带品。
