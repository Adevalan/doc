左值是可以赋值的，
x变量是个左值时，该x变量同时具有左右值，如果使用在等式操作符的左边就是使用x变量的左值。（即内容写入方法）-左值表示 - > 地址 + 写入操作 = 左值
如果在等式操作符的右边就是使用x变量的右值。（即内容读取方法） -> 右值表示 -> 地址 + 读取操作 = 右值
右值数值在程序中也有内存地址，但不存在符号映射，因此无法访问。它只具有右值，它是从寄存器里头取来的，并且它表现为一个实际值。