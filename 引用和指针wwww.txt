int& a = &b; 地址，给了引用，引用本身也是个"容器"，它接收被引用的地址，
改引用名与该地址产生了关联。因此叫做引用。
地址就是其左值，
void go(int& b){
	
}
go(a);
引用 直接引用到目的值，类似于变量。不保存地址，直接与目的地址关系映射。
比指针少了一层关系。
操作引用直接等于操作固定地址上的值。

指针，保存的是地址，具有两层关系。与值不产生关系。
操作指针可能操作到很多值。
指针本身是一个变量，与引用相互关联，
任何变量都是引用的。int a，a就是引用，对一个地址上的一个值关联的，
对一个存储容器相关联的，就叫做引用。
