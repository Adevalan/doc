指令字长指的是元指令长度，
元指令长度根据数据的长度是可变的。
比如 
mov 0
mov 0x00000000
但元指令长度 + 组合长度指令的字长是确定的。
难道RISC就可以等长了？
那不是占有了更多的空间吗。。。默认以最长指令长度为对齐。

而且对齐就是为了等长访问。
计算机无需解析分割、直接确定数据的位置。
当你不知道该指令的长度以及结尾时。
如果以固定的间隔存储位置，那么每个单元格就是一条指令。不需要进行糅合拆开。
