要以schema为单元数据表示单位来访问的话，那么每个用户就必须有默认的表空间schema。


多用户数据库，并不是说，一个应用访问同一个数据文件，需要采用不同的账号来，

我是一个应用，我访问一个数据库，数据库支持多用户，访问数据库时需要给应用配很多的账号，每个启动的应用，
分到一个账号，它们不能并发登录。

如果能够并发登录，那么多用户账号的形式维持应用运转就显得没有意义了，因此它们只能划分作为表空间的作用。
那么各个账号应该拥有各自的作用。
