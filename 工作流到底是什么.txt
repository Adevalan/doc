工作流就是限定了一定的工作流程，想要嵌入工作流程进行工作有好多的方法，
但一定也要符合一定的约定。
比如A级工作流接收了什么，但因为线程安全的问题，它必须new出来，
而B级工作流同样也接收了什么，它保持了接收的内容。

可以抽象继承下来，填充业务方法，也可以获取此Javabean并且置入流程相关的约定属性。
还可以写成静态类（极其不推荐）
另外，最后还可以做成。。。假如要织入业务对象的话要怎么做？
我觉得应该是Setter设置，设置工作流的执行模式，而后由该工作流符合特定模式进行执行，
并且访问其被织入的业务对象。

