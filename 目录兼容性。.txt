目录兼容性，为了Unix规范开发的程序，在linux当中可以运行，所以将linux目录结构和Unix目录的结构差异性并存。
不冲突，目录是一样的，如果目录内访问的文件是不一样的，或者操作系统启动用的不是这些文件就坑爹了。
linux具有自己定义的特性目录，这些特性目录兼容了以往unix程序的开发规范。
使得unix程序只需要重新编译一下，兼容POSIX标准即可。或者不进行重新编译，直接使用。
因为系统调用基本都是模拟的一样。
