指令可以等待，但是指令完成时，添加入先前指令状态列表。
完成指令列表，某指令完成时，Redo列表，指令含参数，用户的输入就是参数。

然后执行Redo次数-1。
这样才是正确的。
a其实算个指令，a指令完成时机在a->插入模式->退出插入模式就行。算一条指令完成。


判断指令完成时，是否回到了先前模式。
是的话就算指令完成。

dd完成后，依旧在当前模式下。输入指令列表。
a完成后，回到当前模式下。算指令完成，输入指令列表。
判断指令是否完成的标准，就是指令是否切换了模式，如果切换了模式，需要等待回到
当前层算作指令完成。
