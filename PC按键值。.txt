PC模式下，只有一些按键可用。

而且游戏会根据键值来进行匹配，
游戏实现不知道上下左右的键值。
由用户自己来设置。

要么就是上下左右的键值游戏里头已经有了。

但是这样的话，就不自由度了。


没有固定键值的情况下，很爽，根据具体的动作，得到其键值。

其键值再次出现时算为一个动作，

除非每款手柄的这个键值都一样。 那么xbox360的这几个按键值的肯定不会一样。

模拟器实现的也是键值映射。

PC游戏不设置键值也行吗。。。默认键值？