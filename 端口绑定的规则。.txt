假设考虑多块真实物理网卡的情况下，
一切都比较明朗了。
真实物理网卡，带来的就是多个IP地址，并且它们之间不应当相互影响。

程序进程的端口绑定应当针对某一块网卡进行。因为其它网卡的网段内不一定会出现
访问同一端口的机器，而且也没有必要去这么做。
既然有多个入口，就应该带来高并发性。

在一个ip地址上绑定网卡,监听端口,就代表这是一台主机，我们可以监视它的一些响应。

一个ip地址就对应了一台网络上的主机。对不对，有没有错?

对不对，没错吧。

