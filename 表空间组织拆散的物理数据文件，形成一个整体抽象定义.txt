拆的约散，io的访问过程越精简。
组织的越多，性能降低的越快。

表空间组织那些拆散的物理数据文件，形成一个整体抽象定义
就如同数据文件是有不同的类型的，日志文件、回滚文件、数据文件、操作日志文件...
这些文件类型中，某些组成一个表空间。形成这个数据库的存储区域。

当然，抽象可以 有意义抽，和无意义抽。

有意义抽 抽出来的还是原来的概念，
无意义抽 抽出来的是全新的概念，只不过需要原有的一些东西来支持它的实现。

表空间又是数据文件的逻辑组合。数据文件才是真正的数据库，但是它是分散的。由表空间的逻辑链表将其结合的它。


