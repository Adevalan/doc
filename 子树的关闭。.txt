随便挂载到一个不会关闭的父进程即可。
因为进程树的原由，每个进程都必须有一个父PID。
这个父PID不管在哪个树，它的根节点肯定是init:1 PPID。

对于进程的关闭，如果关闭，某个树，其树底下的所有子树都会被关闭。

init 1 不会被关闭，ssh-D会被关闭，所以所属的内容都会被关闭。
可挂载到另外的树当中，或者另外创建一个树。
screen的做法既是创建新的进程组。挂载在1。而不是类explorer。
linux下的所有顶级进程，均派生于init1进程。
而没有其它的领头进程。