WebService-SOAP-Restful为客户端抽象出方法。

而使用户程序员不接触底层的包交流。包格式设计等等。
选定一种协议，互相伤害吧！

而http则是因为已经有了http服务器和http浏览器。

而服务端和服务端之间的交流没有服务器和浏览器的概念。
通过HTTP协议获取的都只是请求头，当然也可以通过这个来通信。没什么问题。
服务端、客户端需要解析请求头，提取请求内容，映射url。
这种的叫做web-api。

其它的叫做WebService、
真实的去想它的实现的话，就会觉得它不会/这么简单。的
