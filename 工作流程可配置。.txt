需要某个类的工作流程可配置时，可考虑使用Spring框架来进行配置，
而非是properties文件，

而可配置的情况下，可考虑设计一个配置类，并给定唯一标识放在spring配置文件当中，
也可以配置spring的流程实现类，并要求提供参数，唯一通用处理流程会来spring文件查找指定的bean。
总之都需要通过唯一标识来找，不管放在哪个类，
放在那个类即有意义，上层配置，说明上层的上层还有东西要用到它，
或者该层无需被其它类抓取到，它为单例自工作流程，或它使用代理类方式切入工作流程类。

而配置层，则一定是由其它的来抓取的。