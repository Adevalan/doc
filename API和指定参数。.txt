内核对象用于抽象系统物理资源。
即，你访问文件时，并非在访问区块数据，而它的各方面信息，
都被组织到一个对象当中，方便编程的开发。

比如你访问COM1端口的时候，通过内核对象来代表它。而不是直接进行I/O。


API对应着一种操作，但不指定要操作何种目标。
这样设计的API会更简单。高层开发人员不需要理解端口的概念，
直接调用API即可。


API对应着一种目标。操作系统要设计的尽量通用。
那么API就可以表示一种对象，所有的目标对象都可以继承于一个对象结构类型。

类似于每一种对象都受管于一种对象类型。
