某类继承于Request就有了Request类的所有方法。


由于子类没有重写父类的虚函数方法，
那么当一个子类对象产生时，调用子类的方法，其实就是在调用父类，
但是还有一个问题，下级类不能存上级类的实例，因为下级类没有该方法。


因此要考虑使用包装。
new 一个httprequest就是一个新的httprequest了，
具有父类的方法，但是必须给它一个父类的对象，
子类是不存在父类对象的，
只能做封装。
