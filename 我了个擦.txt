适用于不同情况下，推荐使用不同的接口。
比如equals适合判断是否相等的情况。
Comparable和Comparetor适合进行排序的情况。
还有其它情况下的情况接口，一定不能乱进行混用。

比如equals不仅仅用在比较是否相等的地方，那么一旦涉及非相等比较类型的操作，
在别的情况下使用了equals作为基准，那么就会炸。


因为排序是根据一个条件来进行排序的，而不是根据整个对象来进行排序的。
所以不会使用equals来判断两个对象是否完全相等。
仅仅判断这个字段是否相等，排序和两个对象相等是不一样的。