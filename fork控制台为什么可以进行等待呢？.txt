主控制台执行fork为什么可以等待呢？
因为fork-bash与main-bash之间实现了等待机制。

而普通脚本一般不会进行等待。并且要进行等待的话：
也要符合fork-bash的设计接口。


那么主main-bash为什么要进行等待呢？
因为主main-bash执行脚本过程是阻塞的。
阻塞与等待是两个不相同的概念。

mian-bash并非执行fork-bash，而是fork出了另一个无相关的平等级别进程。
相同级别的进程。处于相同的级别。
因此它们之间其实是隔离的。
无关相互的运行的。都是主进程级别。
平级别的。
