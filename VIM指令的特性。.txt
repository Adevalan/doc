涉及二个问题：
①、如何确定指令的范围。如：a dd dx
指令分为立即执行和复合型指令。

②、指令何时完成。



编辑器的指令缓冲区。
输入一个字符时，比较指令缓冲的Pattern是否符合某一指令格式。
符合的话，就执行指令，并在完成时，插入完成标记。


指令执行解析过程：
先解析指令。
再执行指令。
最后完成指令。

解析指令，即识别指令。
执行指令，即执行过程。
完成指令，即加入操作到输出列表。