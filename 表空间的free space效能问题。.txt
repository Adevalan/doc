 采用分散计算free space的方式会造成不必要的消耗。
在保证数据一致性的情况下，最好的方式必然是索引大块的free space区域进行自由索取，
而不是去计算当前数据量下所存在的间隙空间。
另外表空间table space和segment其实是一个概念，一个segment就代表了一个表空间。
一个表空间由多个数据文件组成，（逻辑上的划分，可能多个数据文件组成一张表，也可能一张表被拆分
到多个数据文件当中。）
而每个数据文件又由不同的extent数据范围区块产生。数据区块即实际记录。
而数据文件则可能是表数据的集散和聚合。