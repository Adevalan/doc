输入一个编码的Bytes[]，以某种其他编码显示使用，
或者以这种错的指定编码转换至另外的编码，将会出错。

都是一个编码数组，以其它编码的使用方式进行使用，都会出错。
要想把一段字符数组转换成其它编码，必须指定字符数组的编码，定义一种字符读取方式。

以字符的方式读取文件的话。
如果读取控制系统一开始就有默认编码的话，那么读取时就需要指定文件的编码，进行存储转换。
如果读取控制系统没有默认编码的话，那么不需要在读取时指定文件编码，仅在显示时向系统进行转换。